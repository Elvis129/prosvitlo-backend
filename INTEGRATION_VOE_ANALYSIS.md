# Аналіз можливості інтеграції Вінницяобленерго (VOE)

## 📊 ВИСНОВОК: ТАК, це можливо з деякими обмеженнями

---

## 1. Аналіз сайту VOE (www.voe.com.ua)

### ✅ Доступні ресурси:
- **Аварійні відключення**: `https://www.voe.com.ua/disconnection/emergency`
- **Планові відключення**: `https://www.voe.com.ua/disconnection/planned`
- **Детальна інформація**: `https://www.voe.com.ua/disconnection/detailed` (403 помилка - потребує авторизації)
- **Графік ГПВ**: `https://www.voe.com.ua/informatsiya-pro-cherhy-hrafika-pohodynnykh-vidklyuchen-hpv-1`

### 📋 Структура даних VOE:

#### Графіки ГПВ:
- Надається **PDF файл** зі зображенням графіка (схоже на HOE)
- URL: `https://www.voe.com.ua/sites/default/files/2026-01/7.7-gpv-voe-2025-26-zima_-sayt.pdf`
- Можна використати OCR парсинг (як існуючий `schedule_ocr_parser.py`)

#### Планові/Аварійні відключення:
- Сторінки містять **фільтри** по року, місяцю, структурній одиниці
- Дані **завантажуються динамічно** через форми
- Схожа структура до HOE (РЕМи, міста, вулиці, будинки)

### ⚠️ Виявлені проблеми:

1. **Сторінка `/disconnection/detailed` повертає 403**
   - Можливо потребує авторизації
   - Можливо заблокована для ботів
   - Треба перевірити через браузер з cookies

2. **Інформація про адреси і черги**
   - У VOE є посилання на сервіс: `https://site.voe.com.ua/informuvannya-spozhyvachiv`
   - Можливо там є пошук за адресою → черга
   - Альтернативно: витягувати з PDF графіка

3. **PDF замість HTML таблиць**
   - Графіки ГПВ - це PDF файли
   - Потребує OCR (як у HOE, але там PNG зображення)

---

## 2. Аналіз існуючого бекенду (HOE)

### ✅ Сильні сторони архітектури:

```
app/
├── scraper/           # Модульна структура парсерів
│   ├── hoe_parser.py          # Парсер для HOE
│   ├── outage_parser.py       # Парсер відключень HOE
│   ├── schedule_parser.py     # Парсер графіків HOE
│   ├── announcements_parser.py # Парсер оголошень HOE
│   └── excel_parser.py        # Парсер Excel файлів
├── models.py          # SQLAlchemy моделі (універсальні)
├── config.py          # Конфігурація (можна розширити)
├── scheduler.py       # Планувальник завдань
└── services/          # Firebase, Telegram
```

### 🎯 Універсальні компоненти (можна перевикористати):

1. **Моделі БД** - підходять для будь-якого обленерго:
   - `EmergencyOutage` - аварійні відключення
   - `PlannedOutage` - планові відключення
   - `Schedule` - графіки ГПВ
   - `AddressQueue` - адреси → черги
   - `DeviceToken` - пристрої користувачів

2. **Сервіси**:
   - `firebase_service.py` - відправка пушів
   - `telegram_service.py` - канал Telegram
   - Можуть працювати з будь-якою областю

3. **Scheduler**:
   - Абстрактна логіка планування
   - Можна запускати паралельно для HOE і VOE

4. **API роути**:
   - Вже підтримують фільтрацію за містом
   - Можна додати фільтр за областю

---

## 3. План інтеграції (Multi-Region Support)

### Архітектурний підхід: **MULTI-PROVIDER**

```
Конфігурація по провайдерах:
├── HOE (Хмельницькобленерго)
│   ├── hoe_parser.py
│   ├── hoe_schedule_parser.py
│   └── hoe_announcements_parser.py
│
└── VOE (Вінницяобленерго)
    ├── voe_parser.py
    ├── voe_schedule_parser.py
    └── voe_announcements_parser.py
```

### 3.1. Додати поле `region` у всі таблиці

**Зміни в моделях**:
```python
# app/models.py

class Schedule(Base):
    # ... існуючі поля
    region = Column(String, default="hoe", nullable=False, index=True)  # "hoe" або "voe"

class EmergencyOutage(Base):
    # ... існуючі поля
    region = Column(String, default="hoe", nullable=False, index=True)

class PlannedOutage(Base):
    # ... існуючі поля
    region = Column(String, default="hoe", nullable=False, index=True)

class AddressQueue(Base):
    # ... існуючі поля
    region = Column(String, default="hoe", nullable=False, index=True)

# Оновлені індекси:
__table_args__ = (
    Index('idx_schedule_region_date', 'region', 'date'),
)
```

### 3.2. Розширити конфігурацію

**Зміни в config.py**:
```python
class Settings(BaseSettings):
    # ... існуючі налаштування
    
    # Multi-region support
    ENABLED_REGIONS: List[str] = ["hoe"]  # ["hoe", "voe"]
    
    # HOE (Хмельницькобленерго)
    HOE_ENABLED: bool = True
    HOE_SCHEDULE_URL: str = "https://hoe.com.ua/page/pogodinni-vidkljuchennja"
    HOE_NEWS_URL: str = "https://hoe.com.ua/post/novini-kompaniji"
    HOE_SHUTDOWN_URL: str = "https://hoe.com.ua/shutdown/eventlist"
    
    # VOE (Вінницяобленерго)
    VOE_ENABLED: bool = False  # За замовчуванням вимкнено
    VOE_SCHEDULE_URL: str = "https://www.voe.com.ua/informatsiya-pro-cherhy-hrafika-pohodynnykh-vidklyuchen-hpv-1"
    VOE_EMERGENCY_URL: str = "https://www.voe.com.ua/disconnection/emergency"
    VOE_PLANNED_URL: str = "https://www.voe.com.ua/disconnection/planned"
```

### 3.3. Створити парсери для VOE

**Нові файли**:
```
app/scraper/
├── providers/
│   ├── __init__.py
│   ├── hoe/                    # Перемістити існуючі
│   │   ├── __init__.py
│   │   ├── hoe_parser.py
│   │   ├── hoe_schedule_parser.py
│   │   └── hoe_announcements_parser.py
│   │
│   └── voe/                    # Нові для VOE
│       ├── __init__.py
│       ├── voe_parser.py       # Аварійні/планові
│       ├── voe_schedule_parser.py  # PDF парсинг
│       └── voe_announcements_parser.py
│
└── base_parser.py              # Базовий клас для всіх
```

**Базовий клас парсера**:
```python
# app/scraper/base_parser.py

from abc import ABC, abstractmethod
from typing import List, Dict

class BaseOutageParser(ABC):
    """Базовий клас для парсерів відключень"""
    
    def __init__(self, region: str):
        self.region = region
    
    @abstractmethod
    def fetch_emergency_outages(self) -> List[Dict]:
        """Парсить аварійні відключення"""
        pass
    
    @abstractmethod
    def fetch_planned_outages(self) -> List[Dict]:
        """Парсить планові відключення"""
        pass

class BaseScheduleParser(ABC):
    """Базовий клас для парсерів графіків"""
    
    def __init__(self, region: str):
        self.region = region
    
    @abstractmethod
    def fetch_schedule_images(self) -> List[Dict]:
        """Завантажує графіки"""
        pass
```

### 3.4. Оновити scheduler для multi-region

**Зміни в scheduler.py**:
```python
# app/scheduler.py

from app.config import settings

def update_schedules_all_regions():
    """Оновлює графіки для всіх увімкнених регіонів"""
    if settings.HOE_ENABLED and "hoe" in settings.ENABLED_REGIONS:
        update_schedules_hoe()
    
    if settings.VOE_ENABLED and "voe" in settings.ENABLED_REGIONS:
        update_schedules_voe()

def update_schedules_hoe():
    """Існуюча логіка для HOE"""
    from app.scraper.providers.hoe import hoe_schedule_parser
    # ... існуючий код

def update_schedules_voe():
    """Нова логіка для VOE"""
    from app.scraper.providers.voe import voe_schedule_parser
    # ... аналогічний код з region="voe"

# Аналогічно для відключень та оголошень
```

### 3.5. Оновити API endpoints

**Додати фільтрацію по region**:
```python
# app/api/schedule_routes.py

@router.get("/schedules")
def get_schedules(
    region: str = Query(default="hoe", description="Region: hoe, voe"),
    date: Optional[str] = None,
    db: Session = Depends(get_db)
):
    query = db.query(Schedule).filter(Schedule.region == region)
    if date:
        query = query.filter(Schedule.date == date)
    return query.all()

# Аналогічно для outages
```

---

## 4. Виклики та рішення

### 🔴 Виклик 1: VOE використовує PDF замість зображень

**Рішення**:
```python
# app/scraper/providers/voe/voe_schedule_parser.py

import requests
from pdf2image import convert_from_bytes
from app.scraper.schedule_ocr_parser import parse_schedule_with_ocr

def fetch_voe_schedule():
    # Завантажуємо PDF
    pdf_url = "https://www.voe.com.ua/sites/default/files/.../gpv.pdf"
    response = requests.get(pdf_url)
    
    # Конвертуємо PDF → зображення
    images = convert_from_bytes(response.content)
    
    # Парсимо кожну сторінку через OCR
    for image in images:
        parsed_data = parse_schedule_with_ocr(image)
        # ... обробка
```

**Потрібна бібліотека**: `pip install pdf2image poppler-utils`

### 🔴 Виклик 2: Інформація про адреси

**Варіанти**:

1. **Парсити з PDF графіка** (якщо там є таблиця адрес)
2. **Зробити окремий парсер для `site.voe.com.ua/informuvannya-spozhyvachiv`**
3. **Дозволити користувачам вводити чергу вручну** (як fallback)

**Рекомендація**: Спочатку дозволити вручну, потім додати автоматичний парсинг.

### 🔴 Виклик 3: Детальна сторінка (403 помилка)

**Рішення**:
- Використати Selenium/Playwright для емуляції браузера
- Або працювати тільки з доступними сторінками (emergency/planned)
- Можливо вони блокують по User-Agent - спробувати різні headers

### 🔴 Виклик 4: Міграції БД без downtime

**Рішення**:
```python
# migrations/003_add_region_field.py

def upgrade(db):
    # 1. Додати поле з default="hoe"
    db.execute("ALTER TABLE schedules ADD COLUMN region VARCHAR DEFAULT 'hoe'")
    db.execute("ALTER TABLE emergency_outages ADD COLUMN region VARCHAR DEFAULT 'hoe'")
    db.execute("ALTER TABLE planned_outages ADD COLUMN region VARCHAR DEFAULT 'hoe'")
    db.execute("ALTER TABLE address_queues ADD COLUMN region VARCHAR DEFAULT 'hoe'")
    
    # 2. Створити індекси
    db.execute("CREATE INDEX idx_schedules_region ON schedules(region)")
    # ... інші індекси
    
    # 3. Оновити існуючі записи (всі HOE)
    db.execute("UPDATE schedules SET region='hoe' WHERE region IS NULL")
```

---

## 5. Поетапне впровадження (БЕЗ простоїв)

### 📅 Етап 1: Підготовка (1-2 дні)
- ✅ Додати поле `region` у моделі з default="hoe"
- ✅ Створити міграцію БД
- ✅ Оновити конфігурацію з `VOE_ENABLED=False`
- ✅ Всі існуючі користувачі HOE не постраждають

### 📅 Етап 2: Створити VOE парсери (3-5 днів)
- ✅ Створити `app/scraper/providers/voe/`
- ✅ Імплементувати `voe_parser.py` (аварійні/планові)
- ✅ Імплементувати `voe_schedule_parser.py` (PDF → OCR)
- ✅ Імплементувати `voe_announcements_parser.py`
- ✅ Протестувати окремо, не запускати в scheduler

### 📅 Етап 3: Інтеграція scheduler (1-2 дні)
- ✅ Оновити `scheduler.py` для multi-region
- ✅ Додати `if VOE_ENABLED` перевірки
- ✅ Запускати VOE jobs тільки якщо `VOE_ENABLED=True`
- ✅ HOE продовжує працювати як зараз

### 📅 Етап 4: API endpoints (1 день)
- ✅ Додати фільтр `region` у всі GET endpoints
- ✅ Default `region=hoe` для зворотної сумісності
- ✅ Додати `/regions` endpoint для списку доступних регіонів

### 📅 Етап 5: Тестування VOE (2-3 дні)
- ✅ Увімкнути `VOE_ENABLED=True` на тестовому сервері
- ✅ Перевірити парсинг, БД, пуші
- ✅ Переконатися що HOE не постраждав

### 📅 Етап 6: Production запуск
- ✅ Deploy з `VOE_ENABLED=True`
- ✅ Моніторинг логів обох регіонів
- ✅ Якщо щось пішло не так - відкат через `VOE_ENABLED=False`

---

## 6. Структура файлів після впровадження

```
app/
├── config.py                     # ✏️ Розширено: VOE налаштування
├── models.py                     # ✏️ Додано: region field
├── scheduler.py                  # ✏️ Multi-region jobs
│
├── scraper/
│   ├── base_parser.py           # 🆕 Базовий клас
│   │
│   ├── providers/
│   │   ├── __init__.py
│   │   │
│   │   ├── hoe/                 # 📦 Існуючі, перенесені
│   │   │   ├── __init__.py
│   │   │   ├── hoe_parser.py
│   │   │   ├── hoe_schedule_parser.py
│   │   │   └── hoe_announcements_parser.py
│   │   │
│   │   └── voe/                 # 🆕 Нові парсери
│   │       ├── __init__.py
│   │       ├── voe_parser.py
│   │       ├── voe_schedule_parser.py
│   │       └── voe_announcements_parser.py
│   │
│   ├── schedule_ocr_parser.py   # Існуючий, universal
│   └── page_cache.py            # Існуючий, universal
│
├── api/
│   ├── schedule_routes.py       # ✏️ Додано: region filter
│   ├── outage_routes.py         # ✏️ Додано: region filter
│   └── regions_routes.py        # 🆕 Список доступних регіонів
│
└── services/
    ├── firebase_service.py      # Без змін
    └── telegram_service.py      # Без змін
```

---

## 7. Оцінка складності

| Компонент | Складність | Час |
|-----------|-----------|------|
| Міграція БД | 🟢 Легко | 1 день |
| VOE парсер відключень | 🟡 Середньо | 2-3 дні |
| VOE парсер графіків (PDF) | 🟠 Складно | 3-5 днів |
| VOE парсер адрес | 🔴 Дуже складно | 5-7 днів* |
| Scheduler multi-region | 🟢 Легко | 1 день |
| API updates | 🟢 Легко | 1 день |
| Тестування | 🟡 Середньо | 3-5 днів |
| **ЗАГАЛОМ** | | **16-25 днів** |

\* _Якщо доведеться парсити з PDF або обходити 403 помилку_

---

## 8. Рекомендації

### ✅ ТАК, це можливо зробити БЕЗ простоїв:

1. **Поле `region` з default="hoe"** - існуючі дані не постраждають
2. **Feature flag `VOE_ENABLED`** - можна вмикати/вимикати
3. **Модульна архітектура** - HOE і VOE незалежні
4. **Поетапний запуск** - можна тестувати окремо

### ⚠️ Обмеження та складнощі:

1. **PDF парсинг** - треба `pdf2image` + OCR (складніше ніж PNG)
2. **403 на детальну інформацію** - можливо треба Selenium
3. **Адреси → черги** - невідомо звідки VOE це бере
4. **Подвійне навантаження** - scheduler працює для 2 регіонів

### 🎯 MVP для VOE (мінімальна версія):

1. ✅ Аварійні відключення (є доступ)
2. ✅ Планові відключення (є доступ)
3. ✅ Графіки ГПВ (PDF → OCR)
4. ⏸️ Адреси → черги (вручну, поки що)
5. ⏸️ Оголошення (якщо є відкрита сторінка)

---

## 9. Альтернативний підхід: Окремий інстанс

Якщо не хочете ускладнювати існуючий бек:

### Варіант: 2 окремі інстанси

```
prosvitlo-hoe-backend.fly.dev     # Хмельницька
prosvitlo-voe-backend.fly.dev     # Вінницька
```

**Плюси**:
- ✅ Повна ізоляція
- ✅ Простіше масштабувати
- ✅ Окремі логи/метрики
- ✅ Незалежні deploy

**Мінуси**:
- ❌ Подвоєна інфраструктура
- ❌ Дублювання коду
- ❌ Складніше підтримувати

---

## 10. Остаточна рекомендація

### 🎯 Рекомендую: **Multi-region в одному бекенді**

**Чому**:
- Існуюча архітектура вже добре структурована
- Моделі БД універсальні
- Scheduler легко розширити
- Одна кодова база = легше підтримувати
- Feature flags дозволяють безпечний запуск

**Починайте з MVP**:
1. Міграція БД + region field
2. VOE парсер аварійних/планових
3. VOE графіки (PDF)
4. Адреси - вручну (додасте автоматизацію пізніше)

**Часова оцінка**: 2-3 тижні до першої робочої версії для VOE.

---

## Питання для уточнення

1. Чи є у вас доступ до особистого кабінету VOE? (для 403 помилки)
2. Чи плануєте підтримувати інші області в майбутньому?
3. Чи є обмеження по ресурсах сервера (CPU/RAM для PDF парсингу)?
4. Чи потрібна окрема база користувачів для VOE, чи загальна?
5. Чи є пріоритет: швидкий MVP VS повний функціонал?
