# 📝 Внутрішні нотатки (не включаються в деплой)

## Що видалено:
- ❌ GitHub Actions автоматичний деплой (.github/)
- ❌ Тестові скрипти (test_*.py)
- ❌ Міграційні скрипти (migrate_*.py)
- ❌ Старі shell скрипти (start*.sh)
- ❌ Застаріла документація (DEPLOY.md, FIX_BASE_URL.md, etc)
- ❌ Логи та локальна база даних

## Важливі зміни:

### 1. Persistent Storage для зображень
- Зображення тепер зберігаються в `/data/static/schedules/`
- Використовується існуючий volume `prosvitlo_data`
- Файли НЕ зникають після деплою

### 2. Test Push виправлено
- Додано `notification_type="tokens"` в API
- Тепер можна відправляти на конкретні FCM токени
- Не відправляє всім користувачам

### 3. Деплой тільки вручну
```bash
fly deploy
```

## Створення нових тестових скриптів (НЕ комітити):

Якщо потрібен тестовий скрипт:
1. Створіть файл з префіксом `test_` (вже в .gitignore)
2. Після тестування видаліть або залишіть локально
3. Ніколи не комітьте в git

## Firebase serviceAccountKey.json
- В .gitignore
- НЕ комітити в репозиторій
- На Fly.io додано як секрет
