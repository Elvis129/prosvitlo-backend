# 📋 План перевірки Push-повідомлень в додатку

## ✅ Результати перевірки Backend:

### 1. **Відправка пушів** ✅
- ✅ `send_to_address_users()` - відправляє тільки користувачам конкретної адреси
- ✅ `send_to_all_users()` - відправляє всім користувачам (broadcast)
- ✅ Тестовий пуш на один девайс працює
- ✅ device_ids зберігаються в історії для персональних повідомлень

### 2. **Фільтрація історії** ✅
- ✅ `get_user_notifications()` правильно фільтрує:
  - Показує ВСІ повідомлення з `type="all"` (загальні)
  - Показує тільки повідомлення де `device_id` є в `device_ids` (персональні)
- ✅ Перевірено на production - фільтрація працює правильно

---

## 🧪 Що потрібно перевірити в ДОДАТКУ:

### 📱 1. Реєстрація FCM токену

**Що перевіряти:**
```kotlin
// Чи відправляється device_id і FCM token при старті додатку?
POST /api/v1/tokens/register
{
  "device_id": "унікальний_id_пристрою",  // ← ВАЖЛИВО!
  "fcm_token": "fcm_token_від_firebase",
  "platform": "android"
}
```

**Можливі проблеми:**
- ❌ device_id не відправляється
- ❌ device_id змінюється при кожному запуску
- ❌ Використовується тимчасовий ID замість постійного

**Як перевірити:**
1. Запустіть додаток
2. Перевірте логи network запитів (Charles, Logcat)
3. Знайдіть запит `POST /api/v1/tokens/register`
4. Переконайтеся що `device_id` відправляється і завжди однаковий

---

### 📍 2. Збереження адрес користувача

**Що перевіряти:**
```kotlin
// Чи зберігається адреса з правильним device_id?
POST /api/v1/addresses/add
{
  "device_id": "той_самий_device_id",  // ← ВАЖЛИВО!
  "city": "Хмельницький",
  "street": "вул. Проскурівська",
  "house_number": "1",
  "queue": "6.2"
}
```

**Можливі проблеми:**
- ❌ device_id не відправляється при збереженні адреси
- ❌ Відправляється інший device_id ніж при реєстрації токену

**Як перевірити:**
1. Додайте адресу в додатку
2. Перевірте логи network запитів
3. Знайдіть запит `POST /api/v1/addresses/add`
4. Переконайтеся що `device_id` такий самий як при реєстрації токену

---

### 📜 3. Отримання історії повідомлень

**Що перевіряти:**
```kotlin
// Чи правильно запитується історія?
GET /api/v1/notifications/{device_id}?limit=50

// ПРАВИЛЬНОЛьно:
GET /api/v1/notifications/AE3A.240806.005?limit=50

// НЕПРАВИЛЬНО:
GET /api/v1/notifications?limit=50  // ← без device_id
```

**Можливі проблеми:**
- ❌ Запит без `device_id` - отримаємо ВСІ повідомлення
- ❌ Використовується `/notifications` замість `/notifications/{device_id}`
- ❌ device_id не передається або передається пустим

**Як перевірити:**
1. Відкрийте історію повідомлень в додатку
2. Перевірте логи network запитів
3. Знайдіть запит до `/api/v1/notifications/...`
4. Переконайтеся що:
   - ✅ URL містить device_id: `/notifications/AE3A.240806.005`
   - ✅ device_id той самий що і при реєстрації

---

## 🔍 Діагностика проблеми "бачу чужі пуші":

### Сценарій 1: Використовується неправильний endpoint
```
❌ GET /api/v1/notifications?limit=50
   Повертає ВСІ повідомлення без фільтрації

✅ GET /api/v1/notifications/{device_id}?limit=50
   Повертає тільки повідомлення для цього користувача
```

### Сценарій 2: device_id не передається
```kotlin
// ❌ НЕПРАВИЛЬНО:
api.getNotifications(limit = 50)

// ✅ ПРАВИЛЬНО:
api.getNotifications(deviceId = getDeviceId(), limit = 50)
```

### Сценарій 3: device_id різний при реєстрації і запиті історії
```
Реєстрація токену:    device_id = "ABC123"
Збереження адреси:    device_id = "ABC123"  ✅
Запит історії:        device_id = "XYZ789"  ❌ ПОМИЛКА!
```

---

## 🛠️ Інструменти для перевірки:

### 1. **Charles Proxy / HTTP Toolkit**
Перехоплюйте всі HTTP запити і перевіряйте:
- Чи відправляється device_id
- Чи він однаковий в усіх запитах
- Який endpoint використовується

### 2. **Android Logcat**
```kotlin
Log.d("PushDebug", "Device ID: $deviceId")
Log.d("PushDebug", "Fetching notifications for: $deviceId")
```

### 3. **Backend логи**
```bash
fly logs -a prosvitlo-backend | grep "device_id\|notifications"
```

---

## 📊 Очікувана поведінка:

### Користувач А (device_id = "DEVICE_A"):
- ✅ Бачить загальні повідомлення (`type="all"`)
- ✅ Бачить повідомлення про відключення на своїй вулиці
- ❌ НЕ бачить повідомлення про відключення на чужій вулиці

### Користувач Б (device_id = "DEVICE_B"):
- ✅ Бачить загальні повідомлення (`type="all"`)
- ✅ Бачить повідомлення про відключення на своїй вулиці
- ❌ НЕ бачить повідомлення користувача А про його вулицю

---

## 🎯 Швидка перевірка:

### Крок 1: Перевірте device_id
```bash
# Запустіть тестовий скрипт
python3 test_history_filter.py
```
Змініть `TEST_DEVICE_ID` на ваш device_id з додатку.

### Крок 2: Подивіться які повідомлення повертаються
Якщо ви бачите:
- Тільки `type="all"` - це нормально, персональних повідомлень просто немає
- `type="address"` де ваш device_id НЕ в `device_ids` - **ПРОБЛЕМА в backend**
- Занадто багато повідомлень - можливо використовується неправильний endpoint

### Крок 3: Перевірте код додатку
1. Знайдіть де викликається API для історії повідомлень
2. Переконайтеся що використовується `/notifications/{device_id}`
3. Переконайтеся що device_id той самий що і при реєстрації

---

## 📝 Чеклист для розробника додатку:

- [ ] device_id генерується один раз і зберігається (не змінюється)
- [ ] device_id відправляється при реєстрації FCM токену
- [ ] device_id відправляється при збереженні адреси
- [ ] device_id використовується при запиті історії: `/notifications/{device_id}`
- [ ] Логування device_id для дебагу
- [ ] Перевірка що один і той самий device_id скрізь

---

## 🐛 Якщо проблема залишається:

1. **Зробіть скріншоти network запитів** (Charles/Logcat)
2. **Надішліть логи**:
   - Запит реєстрації токену
   - Запит збереження адреси
   - Запит отримання історії
3. **Вкажіть device_id** вашого тестового пристрою

Backend готовий і працює правильно! Проблема швидше за все в тому, який endpoint використовується в додатку для отримання історії. 🔍
