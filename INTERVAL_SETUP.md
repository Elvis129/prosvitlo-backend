# Керування інтервалом перевірки сайту

Проект підтримує два режими роботи для оптимізації ресурсів сервера:

## 📊 Режими роботи

### 1. Частий режим (кожні 5 хвилин)
Використовується під час періодів **частих змін** на офіційному сайті.
- Графіки: кожні 5 хвилин
- Аварійні відключення: кожні 5 хвилин
- Оголошення: кожні 5 хвилин
- Сповіщення: кожні 5 хвилин

### 2. Рідкий режим (кожну годину)
Використовується під час періодів **рідких змін** на офіційному сайті.
- Графіки: кожну годину
- Аварійні відключення: кожну годину
- Оголошення: кожну годину
- Сповіщення: кожну годину

**Планові відключення** завжди парсяться 1 раз на день о 9:00 незалежно від режиму.

---

## 🚀 Локальний запуск

### Частий режим (5 хвилин):
```bash
./start_frequent.sh
```

### Рідкий режим (1 година):
```bash
./start_hourly.sh
```

### Кастомний інтервал:
```bash
export CHECK_INTERVAL_MINUTES=15
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

---

## ☁️ Розгортання на Fly.io

### Спосіб 1: Через змінні середовища Fly.io (РЕКОМЕНДОВАНО)

Це найкращий спосіб, бо ви можете змінювати інтервал без перезавантаження сервера.

#### Встановити частий режим (5 хвилин):
```bash
fly secrets set CHECK_INTERVAL_MINUTES=5
```

#### Встановити рідкий режим (1 година):
```bash
fly secrets set CHECK_INTERVAL_MINUTES=60
```

#### Переглянути поточні налаштування:
```bash
fly secrets list
```

#### Перезапустити додат��к після зміни:
```bash
fly apps restart
```

**Важливо:** Після зміни змінної середовища через `fly secrets set`, додаток автоматично перезапуститься і нові налаштування будуть застосовані.

---

### Спосіб 2: Через fly.toml (альтернативний)

Відредагуйте файл `fly.toml` і додайте змінну в секцію `[env]`:

```toml
[env]
  CHECK_INTERVAL_MINUTES = "60"  # для рідкого режиму
  # або
  # CHECK_INTERVAL_MINUTES = "5"  # для частого режиму
```

Після зміни розгорніть додаток:
```bash
fly deploy
```

---

## 🔍 Перевірка поточного режиму

Після запуску сервера перевірте логи:

### Локально:
Логи будуть показані в консолі при старті.

### На Fly.io:
```bash
fly logs
```

Ви побачите повідомлення:
```
✅ Планувальник запущено:
  📅 Графіки: кожні 5 хвилин     (або кожні 60 хвилин)
  ⚠️ Аварійні відключення: кожні 5 хвилин
  📢 Оголошення з сайту: кожні 5 хвилин
  ...
```

---

## 💡 Рекомендації

1. **Використовуйте частий режим (5 хв)** коли:
   - Очікуються часті оновлення графіків
   - Період підвищеної активності (аварійні ситуації)
   - Важливий період для користувачів

2. **Використовуйте рідкий режим (60 хв)** коли:
   - Ситуація стабільна
   - Зміни на сайті рідкі
   - Потрібно зекономити ресурси сервера
   - Зменшити навантаження на офіційний сайт

3. **База даних зберігається:**
   - Дані НЕ обнуляються при зміні режиму
   - Зміна інтервалу НЕ впливає на існуючі дані
   - Всі сповіщення та історія зберігаються

---

## 🛠️ Технічні деталі

### Змінна середовища
- **Назва:** `CHECK_INTERVAL_MINUTES`
- **За замовчуванням:** `60` (1 година)
- **Допустимі значення:** будь-яке додатнє число хвилин

### Файли конфігурації
- `app/config.py` - параметр `CHECK_INTERVAL_MINUTES`
- `app/scheduler.py` - використовує цей параметр для налаштування інтервалів
- `Dockerfile` - встановлює значення за замовчуванням

### Скрипти запуску
- `start_frequent.sh` - встановлює `CHECK_INTERVAL_MINUTES=5`
- `start_hourly.sh` - встановлює `CHECK_INTERVAL_MINUTES=60`

---

## ❓ FAQ

**Q: Чи зберігаються дані при зміні інтервалу?**
A: Так, абсолютно всі дані зберігаються. База даних не змінюється.

**Q: Чи потрібно перезавантажувати сервер після зміни?**
A: Так, після зміни змінної середовища через `fly secrets set` додаток автоматично перезапуститься.

**Q: Як швидко застосовуються зміни?**
A: Одразу після перезапуску сервера (1-2 хвилини на Fly.io).

**Q: Чи можна встановити інший інтервал (наприклад, 15 хвилин)?**
A: Так, встановіть будь-яке число: `fly secrets set CHECK_INTERVAL_MINUTES=15`

**Q: Що станеться, якщо не встановити змінну?**
A: Буде використано значення за замовчуванням: 60 хвилин (1 година).
